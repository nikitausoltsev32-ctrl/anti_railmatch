---
phase: 01-stabilizaciya
plan: "05"
type: execute
wave: 1
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - anti_railmatch/components/BidModal.jsx
autonomous: false
gap_closure: true
requirements: [STAB-04]
must_haves:
  truths:
    - "Bid for 1 wagon requires minimum price of 45,000 per wagon"
    - "Bid for >3 wagons requires minimum price of 10,000 per wagon"
    - "Bid for 2-3 wagons requires minimum price of 10,000 per wagon"
    - "The old flat 100,000 minimum is not enforced anywhere"
    - "Validation message shows the correct minimum price"
  artifacts:
    - path: "anti_railmatch/components/BidModal.jsx"
      provides: "Bid price validation with per-wagon minimum"
      contains: "MIN_PRICE"
  key_links:
    - from: "anti_railmatch/components/BidModal.jsx"
      to: "anti_railmatch/app.jsx handleBidSubmit"
      via: "onConfirm callback"
      pattern: "onConfirm.*price.*wagons"
---

<objective>
Fix minimum bid price validation logic.

Purpose: The E2E pipeline test reported "old 100,000 minimum price rule is still enforced." The correct rule: 1 wagon -> 45,000 min per wagon; >3 wagons -> 10,000 min per wagon. The flat 100,000 rule must not exist anywhere. BidModal.jsx may already have partial fix but the validation message and edge cases (2-3 wagons) need verification and correction.
Output: Correct bid price validation matching the product owner's rules.
</objective>

<execution_context>
@C:/Users/HomePc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HomePc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilizaciya/01-03-SUMMARY.md
</context>

<interfaces>
<!-- From anti_railmatch/components/BidModal.jsx (full file, 56 lines) -->

Current validation (line 10-12):
```jsx
const MIN_PRICE = Number(wagons) === 1 ? 45_000 : 10_000;
const MAX_PRICE = 10_000_000;
const isValid = Number(price) >= MIN_PRICE && Number(price) <= MAX_PRICE && Number(wagons) > 0 && Number(wagons) <= maxWagons;
```

Validation message (line 27):
```jsx
{price !== '' && Number(price) < MIN_PRICE && <p>Минимальная цена — {MIN_PRICE.toLocaleString()} ₽</p>}
```

Product owner rule (from STATE.md decision 2026-03-23):
- 1 wagon -> 45,000 min per wagon
- >3 wagons -> 10,000 per wagon min
- Flat 100,000 must be removed

Note: price field is "per wagon" (label says "Цена за 1 вагон"), so MIN_PRICE is compared directly to per-wagon price.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix BidModal minimum price validation</name>
  <files>anti_railmatch/components/BidModal.jsx</files>
  <action>
Read BidModal.jsx fully. Verify the minimum price logic matches the product owner's rules:

**Rules (per-wagon price):**
- 1 wagon: min 45,000 per wagon
- 2-3 wagons: min 10,000 per wagon (implicit — no special rule, use the base minimum)
- >3 wagons: min 10,000 per wagon

Current code: `const MIN_PRICE = Number(wagons) === 1 ? 45_000 : 10_000;`
This is CORRECT — it applies 45,000 for exactly 1 wagon and 10,000 for everything else.

**However, the pipeline test said "old 100,000 rule is still enforced."** This means either:
(a) The code wasn't deployed at test time, OR
(b) There's validation elsewhere (e.g., in handleBidSubmit in app.jsx, or a DB constraint)

Search the entire codebase for any remaining 100000/100_000 validation. Check:
1. `app.jsx` handleBidSubmit function (around line 548) — does it validate price before insert?
2. Any Supabase Edge Function that validates bids
3. Any DB CHECK constraint on bids.price

If handleBidSubmit in app.jsx has its own price validation with 100,000, remove it. The validation should only be in BidModal.jsx (frontend gate).

If the code is already correct, ensure it actually gets built and deployed. Run `npx vite build` to verify no build errors.

Also grep for any hardcoded 100000 references in the entire anti_railmatch directory (excluding node_modules).
  </action>
  <verify>
    <automated>cd "C:/Users/HomePc/Railmatch Anti" && grep -rn "100000\|100_000" anti_railmatch/ --include="*.jsx" --include="*.js" --exclude-dir=node_modules 2>&1 | grep -v "Math\.\|random\|60000000\|1000000\b" || echo "No 100000 price references found"</automated>
  </verify>
  <done>No 100,000 flat minimum price rule exists anywhere in the codebase. BidModal validates: 1 wagon -> 45,000 min; all others -> 10,000 min per wagon. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify bid price validation in browser</name>
  <files>anti_railmatch/components/BidModal.jsx</files>
  <action>Human verifies the bid price validation works correctly in the live app.</action>
  <what-built>Verified/fixed bid price validation to use correct per-wagon minimums.</what-built>
  <how-to-verify>
    1. Open the app, log in as an Owner
    2. Find an open request on the exchange
    3. Click "Откликнуться" to open the bid modal
    4. Test with 1 wagon: enter price 44,999 — should show red validation "Минимальная цена — 45 000 ₽" and button disabled
    5. Enter price 45,000 with 1 wagon — button should become enabled
    6. Change wagons to 4: enter price 9,999 — should show "Минимальная цена — 10 000 ₽" and button disabled
    7. Enter price 10,000 with 4 wagons — button should become enabled
    8. Submit the bid — it should save to database without error
  </how-to-verify>
  <verify>Human confirms validation rules are correct</verify>
  <done>Owner can place bids following new pricing rules. Validation messages match the rules.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- No references to 100,000 flat minimum in any source file
- BidModal correctly computes MIN_PRICE based on wagon count
- Validation message shows correct minimum
- Bid can be submitted when price meets the minimum
</verification>

<success_criteria>
Owner can place bids following the new pricing rules. 1-wagon bids require 45,000+ per wagon, multi-wagon bids require 10,000+ per wagon. No remnants of the old 100,000 flat rule.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilizaciya/01-05-SUMMARY.md`
</output>
