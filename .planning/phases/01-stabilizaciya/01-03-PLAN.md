---
phase: 01-stabilizaciya
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified: []
autonomous: false
requirements:
  - STAB-02
  - STAB-03
  - STAB-04
  - STAB-05

must_haves:
  truths:
    - "Migration 22 applied in Supabase production without errors"
    - "RLS enabled on requests, bids, profiles, messages — confirmed via Supabase Dashboard"
    - "Shipper cannot see bids on other shippers' requests"
    - "Owner can see only their own bids"
    - "Realtime event received in second browser tab when record created in first"
    - "Full pipeline completes: request created → bid placed → chat opened → commission agreed → contacts_revealed = true"
    - "UserDashboard buttons navigate correctly"
  artifacts:
    - path: "migrations/22_enable_rls_final_policies.sql"
      provides: "Applied in Supabase production"
      contains: "ENABLE ROW LEVEL SECURITY"
  key_links:
    - from: "Supabase RLS"
      to: "bids table"
      via: "SELECT policy with ownerId + requestId subquery"
      pattern: "Bid participants can view bids"
---

<objective>
Apply migration 22, verify RLS is active, test Realtime subscriptions, and walk through the complete deal pipeline end-to-end.

Purpose: All code changes from plans 01 and 02 exist but nothing is live until migration 22 is executed in Supabase production. This plan is the human-gated verification step that confirms all Phase 1 requirements.

Output: Production Supabase has RLS enabled on all 4 tables, Realtime works for profiles, full deal pipeline is confirmed working.
</objective>

<execution_context>
@C:/Users/HomePc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HomePc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-stabilizaciya/01-CONTEXT.md
@.planning/phases/01-stabilizaciya/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Apply migration 22 in Supabase Dashboard</name>
  <action>Human applies migration 22 manually via Supabase SQL Editor — no CLI available for this step.</action>
  <what-to-do>
1. Open Supabase Dashboard → project xakyjvlxypivrmuehsxl → SQL Editor
2. Open file migrations/22_enable_rls_final_policies.sql from this repo
3. Paste the entire contents into SQL Editor
4. Click "Run"
5. Confirm there are NO errors in the output (warnings about "relation does not exist" for DROP IF EXISTS are fine — they just mean the policy didn't exist before)
6. Verify RLS is enabled: go to Database → Tables → click each of requests, bids, profiles, messages → confirm "Row Level Security" toggle shows as ON
  </what-to-do>
  <verify>Supabase Dashboard → Database → Tables shows RLS enabled on requests, bids, profiles, messages</verify>
  <done>Migration ran without errors; RLS toggle ON for all 4 tables in Supabase Dashboard</done>
  <resume-signal>Type "applied" when migration ran without errors and RLS shows as enabled on all 4 tables</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify RLS policies and Realtime</name>
  <action>Run SQL queries in Supabase SQL Editor and test Realtime in two browser tabs.</action>
  <what-built>Migration 22 applied RLS policies + Realtime publication update for profiles</what-built>
  <how-to-verify>
**Test A — RLS policies in SQL Editor:**
```sql
-- Test 1: As authenticated user, verify you can see requests
SELECT count(*) FROM public.requests;
-- Expected: returns a number (not permission denied)

-- Test 2: Verify bids SELECT policy exists
SELECT policyname, cmd FROM pg_policies
WHERE tablename IN ('requests','bids','profiles','messages')
ORDER BY tablename, cmd;
-- Expected: shows policies for all 4 tables, SELECT/INSERT/UPDATE as appropriate

-- Test 3: Verify Realtime publication includes profiles
SELECT tablename FROM pg_publication_tables
WHERE pubname = 'supabase_realtime';
-- Expected: includes profiles, requests, bids, messages
```

**Test B — Realtime in browser:**
1. Open app in two browser tabs, both logged in (same or different accounts)
2. In Tab 1: create a new request
3. In Tab 2: observe the request appears in the catalog without page refresh
4. Expected: request appears in Tab 2 within ~2 seconds

**Test C — RLS isolation:**
1. Log in as Owner (Tab 1), log in as different Shipper (Tab 2)
2. Shipper creates a request in Tab 2
3. Owner makes a bid in Tab 1
4. Log in as a third user (different shipper) — they should NOT see the bid on that request
5. Expected: bids are only visible to the owner who made them and the shipper who owns the request
  </how-to-verify>
  <verify>pg_policies shows SELECT policy on bids; pg_publication_tables includes profiles; new request appears in second tab within 2 seconds</verify>
  <done>RLS policies confirmed active; Realtime propagates events across tabs; bids isolated to deal participants only</done>
  <resume-signal>Type "verified" if all tests pass, or describe any failures</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: E2E pipeline test</name>
  <action>Walk through the full deal pipeline manually using two browser sessions.</action>
  <what-built>Complete deal pipeline: request creation through contact reveal</what-built>
  <how-to-verify>
Use two browser sessions (or two accounts) — one as Shipper, one as Owner. Test on production URL.

**Full pipeline steps:**
1. Shipper: create a new request (any station/cargo) → confirm it appears in My Requests
2. Owner: find the request in the catalog → click "Откликнуться" → submit a bid
3. Shipper: check My Requests → see the new bid → click "Открыть чат"
4. Both sides: in the chat, each clicks "Подтвердить условия" → confirm system message "Условия согласованы обеими сторонами"
5. One side: propose commission mode (split or full)
6. Other side: approve the commission proposal → confirm system message about agreed mode
7. One side: click "Оплатить комиссию" → confirm contacts_revealed flag changes and contact info becomes visible

**Also test UserDashboard navigation:**
8. Navigate to view='my-dashboard' (add ?view=my-dashboard to URL or navigate via code)
9. Shipper: click "Создать заявку" → confirm view changes to create form
10. Owner: click "Флот онлайн" → confirm FleetDislocation loads; click "Мои ставки" → confirm MyBidsView loads
  </how-to-verify>
  <verify>All 10 pipeline steps complete without console errors or blank screens</verify>
  <done>Full pipeline completes: request → bid → chat → commission agreement → contacts_revealed = true; UserDashboard buttons navigate to correct views</done>
  <resume-signal>Type "pipeline-ok" if all steps pass, or describe failures with step numbers</resume-signal>
</task>

</tasks>

<verification>
Phase 1 complete when:
- RLS enabled on requests, bids, profiles, messages (visible in Supabase Dashboard)
- profiles in supabase_realtime publication
- Realtime events propagate between browser tabs
- Full deal pipeline completes without errors
- UserDashboard buttons navigate correctly
</verification>

<success_criteria>
All 5 STAB-02..06 requirements satisfied:
- STAB-02: Migration 22 applied, all tables/columns confirmed present
- STAB-03: Realtime delivers events for requests, bids, messages, profiles
- STAB-04: End-to-end pipeline: request → bid → chat → commission → contacts revealed
- STAB-05: RLS active — owner can't modify another shipper's request, shipper can't see another shipper's bids
- STAB-06: All 4 UserDashboard action buttons navigate to correct views
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilizaciya/01-03-SUMMARY.md`
</output>
