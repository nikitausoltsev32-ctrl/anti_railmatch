---
phase: 02-telegram-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [app.jsx, components/MyRequestsView.jsx]
autonomous: true
requirements: [NOTIF-02, NOTIF-03]
must_haves:
  truths:
    - "Sending a chat message triggers sendNotification to the partner (not the sender)"
    - "Shipper sees a 'Принять ставку' button on pending bids in their active requests tab"
    - "Clicking 'Принять ставку' sets bid.status to 'accepted', sends Telegram notification to owner, and navigates to chat"
    - "Accepted bids show 'Открыть чат' button instead of 'Принять ставку'"
  artifacts:
    - path: "app.jsx"
      provides: "handleSendMessage with notification + handleAcceptBid function"
      contains: "sendNotification.*partnerId"
    - path: "components/MyRequestsView.jsx"
      provides: "Accept bid button in active requests tab"
      contains: "onAcceptBid"
  key_links:
    - from: "app.jsx handleSendMessage"
      to: "sendNotification(partnerId, ...)"
      via: "After successful supabase messages insert at line 730"
      pattern: "sendNotification.*partnerId"
    - from: "components/MyRequestsView.jsx bid card"
      to: "app.jsx handleAcceptBid"
      via: "onAcceptBid prop callback"
      pattern: "onAcceptBid"
    - from: "app.jsx handleAcceptBid"
      to: "supabase bids table"
      via: "supabase.from('bids').update({ status: 'accepted' })"
      pattern: "status.*accepted"
---

<objective>
Add two missing notification wires: (1) NOTIF-02 — sendNotification to partner after every chat message send; (2) NOTIF-03 — new handleAcceptBid function and "Принять ставку" button in MyRequestsView.

Purpose: These are the only two requirements needing code changes. NOTIF-01 and NOTIF-04 are already in code; this plan closes the remaining gap.

Output: Modified app.jsx and components/MyRequestsView.jsx.
</objective>

<execution_context>
@C:/Users/HomePc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HomePc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-telegram-notifications/02-CONTEXT.md
@.planning/phases/02-telegram-notifications/02-RESEARCH.md

<interfaces>
<!-- Exact locations and signatures extracted from codebase. -->

app.jsx:140 — sendNotification signature:
```javascript
sendNotification(toUserId: string, subject: string, bodyText: string): void
// Fire-and-forget. Never blocks. Never throws.
```

app.jsx:618 — handleSendMessage (NOTIF-02 insertion point):
```javascript
const handleSendMessage = async (chatId, text) => {
    // ... violation checks ...
    // Line 724-730: legitimate message path:
    const { error } = await supabase.from('messages').insert([{
        chat_id: chatId,
        sender_id: sbUser.id,
        text: text.trim(),
    }]);
    if (error) throw error;
    // INSERT sendNotification HERE — after line 730, before closing try block
};
// activeChat is in scope as component state: { ownerId, shipperInn, ... }
// userProfile.role is in scope: 'shipper' | 'owner'
// sbUser.id is the sender
```

partnerId formula (verified in 4+ places):
```javascript
const partnerId = userProfile.role === 'shipper' ? activeChat?.ownerId : activeChat?.shipperInn;
```
Guard: `if (partnerId && partnerId !== sbUser.id)` — prevents self-notification

app.jsx:527 — buildChatObject signature:
```javascript
buildChatObject(bid: object, req: object, profilesList: array): ChatObject
// Returns bid merged with request fields + profile names
```

app.jsx:1518-1534 — MyRequestsView call site (add onAcceptBid prop here):
```jsx
<MyRequestsView
    requests={requests}
    bids={bids}
    userInn={sbUser?.id}
    userRole={userProfile?.role}
    userId={sbUser?.id}
    profiles={profiles}
    setView={setView}
    onAccept={handleConfirmDeal}   // DO NOT CHANGE — this is deal-terms confirmation
    onChat={(b) => { ... }}
    onAiCreate={handleAiCreate}
    onCancelRequest={handleCancelRequest}
    // ADD: onAcceptBid={(bidId) => handleAcceptBid(bidId)}
/>
```

components/MyRequestsView.jsx:5 — props destructuring (add onAcceptBid):
```javascript
export default function MyRequestsView({ requests, bids, userInn, userRole, userId, profiles, setView, onAccept, onChat, onAiCreate, onCancelRequest })
```

components/MyRequestsView.jsx:268-295 — bid cards in 'active' tab (add button here):
```jsx
// Currently each bid card ends with:
<button onClick={() => onChat && onChat(bid)} className="...">
    <MessageSquare className="w-4 h-4" /> Начать обсуждение
</button>
// Replace with conditional: pending → "Принять ставку", accepted → "Открыть чат"
```

WARNING: handleConfirmDeal at app.jsx:765 sets shipper_confirmed/owner_confirmed for deal TERMS.
handleAcceptBid is a NEW function that sets bid.status = 'accepted'. Do NOT reuse handleConfirmDeal.
Bid status flow: pending → accepted (this phase) → commission_pending (handleConfirmDeal) → contacts_revealed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sendNotification to handleSendMessage (NOTIF-02)</name>
  <files>app.jsx</files>
  <action>
In `app.jsx`, inside `handleSendMessage`, after the successful message insert (after `if (error) throw error;` at line 730), add the sendNotification call:

```javascript
// After: if (error) throw error;
const partnerId = userProfile.role === 'shipper'
    ? activeChat?.ownerId
    : activeChat?.shipperInn;
if (partnerId && partnerId !== sbUser.id) {
    sendNotification(
        partnerId,
        'Новое сообщение — RailMatch',
        'У вас новое сообщение на платформе RailMatch.\n\nОткройте платформу, чтобы ответить.'
    );
}
```

Do NOT change the function signature. Do NOT add partnerId as a parameter. `activeChat` is already in scope as component state. The guard `partnerId !== sbUser.id` prevents self-notification if ownerId === sbUser.id edge case.
  </action>
  <verify>
    <automated>MISSING — no automated test framework. Code review: grep -n "sendNotification" app.jsx to confirm call exists inside handleSendMessage after the insert.</automated>
  </verify>
  <done>
    `grep -n "sendNotification" app.jsx` shows a call inside handleSendMessage (around line 731+). The call uses `partnerId` derived from `userProfile.role` and `activeChat`. No self-notification (guard present).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add handleAcceptBid + "Принять ставку" button (NOTIF-03)</name>
  <files>app.jsx, components/MyRequestsView.jsx</files>
  <action>
**In app.jsx — add handleAcceptBid function after handleSendMessage (around line 736):**

```javascript
const handleAcceptBid = async (bidId) => {
    if (!sbUser || userProfile?.role !== 'shipper') return;

    const bid = bids.find(b => b.id === bidId);
    if (!bid) return;

    const { data: updatedBid, error } = await supabase
        .from('bids')
        .update({ status: 'accepted' })
        .eq('id', bidId)
        .select()
        .single();

    if (error) {
        showToast('Ошибка при принятии ставки', 'error');
        return;
    }

    setBids(prev => prev.map(b => b.id === bidId ? { ...b, status: 'accepted' } : b));

    sendNotification(
        bid.ownerId,
        'Ваша ставка принята — RailMatch',
        `Компания «${userProfile.company}» приняла вашу ставку.\n\nОткройте платформу, чтобы перейти в чат.`
    );

    const req = requests.find(r => r.id === bid.requestId);
    setActiveChat(buildChatObject(updatedBid, req, profiles));
    setView('chat');
};
```

**In app.jsx — add onAcceptBid prop to MyRequestsView at lines 1518-1534:**

Add `onAcceptBid={(bidId) => handleAcceptBid(bidId)}` to the MyRequestsView JSX. Do NOT remove onAccept={handleConfirmDeal} — that prop is used for deal terms confirmation in a different flow.

**In components/MyRequestsView.jsx — add onAcceptBid to props destructuring (line 5):**

```javascript
export default function MyRequestsView({ requests, bids, userInn, userRole, userId, profiles, setView, onAccept, onChat, onAiCreate, onCancelRequest, onAcceptBid }) {
```

**In components/MyRequestsView.jsx — replace the "Начать обсуждение" button in the active tab bid cards (around line 290) with conditional buttons:**

```jsx
{bid.status === 'pending' && onAcceptBid && (
    <button
        onClick={() => onAcceptBid(bid.id)}
        className="mt-4 w-full px-6 py-3 rounded-2xl bg-emerald-500 hover:bg-emerald-600 text-white font-bold transition-all active:scale-95 flex items-center justify-center gap-2"
    >
        Принять ставку
    </button>
)}
{bid.status === 'accepted' && (
    <button
        onClick={() => onChat && onChat(bid)}
        className="mt-4 w-full px-6 py-3 rounded-2xl bg-blue-600 hover:bg-blue-700 text-white font-bold transition-all active:scale-95 flex items-center justify-center gap-2"
    >
        <MessageSquare className="w-4 h-4" /> Открыть чат
    </button>
)}
```

The existing "Начать обсуждение" button currently at line 290 is in the active tab bid cards. Replace it with the above conditional. The "my-responses" tab button at line 357-360 is SEPARATE — leave it unchanged (that shows owner's own bids, no accept action needed there).
  </action>
  <verify>
    <automated>MISSING — no automated test framework. Code review: grep -n "handleAcceptBid\|onAcceptBid\|Принять ставку" app.jsx components/MyRequestsView.jsx</automated>
  </verify>
  <done>
    `grep -n "handleAcceptBid" app.jsx` shows both the function definition and the prop pass.
    `grep -n "onAcceptBid\|Принять ставку" components/MyRequestsView.jsx` shows prop destructuring and button render.
    handleAcceptBid updates bid status to 'accepted', calls sendNotification(bid.ownerId, ...), then navigates to chat.
  </done>
</task>

</tasks>

<verification>
- `grep -n "sendNotification" app.jsx` shows 4 calls: line ~606 (NOTIF-01), line ~731 (NOTIF-02, new), line ~760+ (NOTIF-03, new), line ~976 (NOTIF-04)
- `grep -n "handleAcceptBid" app.jsx` shows function definition + prop pass to MyRequestsView
- `grep -n "Принять ставку" components/MyRequestsView.jsx` shows button in active tab bid cards
- `grep -n "onAcceptBid" components/MyRequestsView.jsx` shows both prop destructuring and button onClick
</verification>

<success_criteria>
NOTIF-02: Every chat message send triggers sendNotification to the partner — confirmed by code review.
NOTIF-03: Shipper can click "Принять ставку" on any pending bid, which sets status=accepted, notifies owner via Telegram, and navigates to chat — confirmed by code review and visual check.
</success_criteria>

<output>
After completion, create `.planning/phases/02-telegram-notifications/02-02-SUMMARY.md`
</output>
