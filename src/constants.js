// Единое место для бизнес-констант платформы.
// Изменение здесь автоматически применяется везде.

/** Ставка комиссии платформы (2.5%). */
export const PLATFORM_COMMISSION_RATE = 0.025;

/** Допустимые MIME-типы для загрузки верификационных документов. */
export const ALLOWED_DOC_TYPES = ['application/pdf', 'image/jpeg', 'image/png', 'image/webp'];

/** Максимальный размер загружаемого документа в байтах (10 МБ). */
export const MAX_DOC_SIZE_BYTES = 10 * 1024 * 1024;

// --- Санкции за нарушения чата ---
/** Порог: предупреждение */
export const VIOLATION_WARNING = 1;
/** Порог: блокировка чата на 24 часа */
export const VIOLATION_CHAT_BLOCK = 2;
/** Порог: снятие верификации */
export const VIOLATION_UNVERIFY = 3;
/** Порог: полный бан аккаунта */
export const VIOLATION_BAN = 4;
/** Длительность блокировки чата в часах */
export const CHAT_BLOCK_HOURS = 24;
/** Дней без нарушений для сброса баллов */
export const VIOLATION_RESET_DAYS = 30;

/** Максимальное количество раундов переговоров по комиссии */
export const MAX_COMMISSION_ROUNDS = 4;

/**
 * Минимально допустимая ставка за вагон (₽).
 * Ниже рыночного минимума — заявка не принимается.
 * Средний рынок РЖД: полувагон ~80–150к, крытый ~60–120к, цистерна ~100–200к.
 * Установлен консервативный нижний порог для защиты от занижения базы комиссии.
 */
export const MIN_PRICE_PER_WAGON = 30000;
